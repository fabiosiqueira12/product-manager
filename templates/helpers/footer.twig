<footer class="footer">
    <div class="container-fluid">
        <div class="row text-xs-center">
            <div class="col-sm-4 text-sm-left mb-sm-0">
                {{ 'now'|date('Y') }} © <strong>{{dados.title}}</strong>
            </div>
        </div>
    </div>
</footer>