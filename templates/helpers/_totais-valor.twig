<div class="row">
    <div class="col-xl-4 col-lg-4 col-not-4 col-md-12 col-xs-12">
        <div class="box box-block bg-white tile tile-1 mb-2">
            <div class="t-icon right"><span class="bg-primary"></span><i class="ti-money"></i></div>
            <div class="t-content">
                <h6 class="text-uppercase font-weight-bold mb-1">Total do Mês {{return_months("now"|date('n'))}}</h6>
                <h1 style="margin-bottom : 10px">{{price_format(totais['month'])}}</h1>
                <p class="font-weight-bold text-uppercase">Valor de pedidos com status {{finalizado is not empty ? 'finalizado' : 'criado'}}!</p>
            </div>
        </div>
    </div>
    <div class="col-xl-4 col-lg-4 col-not-4 col-md-12 col-xs-12">
        <div class="box box-block bg-white tile tile-1 mb-2">
            <div class="t-icon right"><span class="bg-info"></span><i class="ti-money"></i></div>
            <div class="t-content">
                <h6 class="text-uppercase font-weight-bold mb-1">Total do ano {{"now"|date('Y')}}</h6>
                <h1 style="margin-bottom : 10px">{{price_format(totais['year'])}}</h1>
                <p class="font-weight-bold text-uppercase">Valor de pedidos com status {{finalizado is not empty ? 'finalizado' : 'criado'}}!</p>
            </div>
        </div>
    </div>
     <div class="col-xl-4 col-lg-4 col-not-4 col-md-12 col-xs-12">
        <div class="box box-block bg-white tile tile-1 mb-2">
            <div class="t-icon right"><span class="bg-black"></span><i class="ti-money"></i></div>
            <div class="t-content">
                <h6 class="text-uppercase font-weight-bold mb-1">Valor Total</h6>
                <h1 style="margin-bottom : 10px">{{price_format(totais['all'])}}</h1>
                <p class="font-weight-bold text-uppercase">Valor de pedidos com status {{finalizado is not empty ? 'finalizado' : 'criado'}}!</p>
            </div>
        </div>
    </div>
</div>